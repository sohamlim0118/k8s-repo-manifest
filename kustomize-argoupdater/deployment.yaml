apiVersion: apps/v1
kind: Deployment
metadata:
  name: myweb-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample
  template:
    metadata:
      labels:
        app: sample
    spec:
      containers:
      - image: docker.io/dbgurum/argoupdater:nginx19a
        name: sample
        ports:
        - containerPort: 5000
